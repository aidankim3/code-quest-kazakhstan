# Claude Instructions

Before doing any work:
1. Read `README.md`.
2. Read `AI_WORKFLOW.md`.
3. Read `HANDOFF.md`.
4. Inspect the latest relevant commits/branch state.

Treat GitHub as shared memory with ChatGPT/Codex. Resume from the latest pushed checkpoint rather than relying on private chat history.

Before stopping, especially when token/context budget is low: run relevant checks, commit, push, and update `HANDOFF.md` with exact next steps and current status.

Follow `AI_WORKFLOW.md` for takeover, branches, checkpoints, and conflict avoidance.
